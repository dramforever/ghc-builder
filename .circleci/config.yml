version: 2
jobs:
  build:
    docker:
      - image: terrorjack/vanilla:circleci
    steps:
      - checkout
      - run:
          name: Setup
          command: |
            nix-channel --update

      - restore_cache:
          keys:
            - v3-ghc-source-

      - run:
          name: Get source
          command: |
            [[ -d ghc ]] || git clone --recursive git://git.haskell.org/ghc.git
            cd ghc
            git fetch origin
            git checkout $(cat ../rev.txt)
            cd ..

      - save_cache:
          key: v3-ghc-source-{{ checksum "rev.txt" }}
          paths:
            - ghc

      - restore_cache:
          keys:
            - v2-nix-store-dump-
      - run:
          name: Install dependencies
          command: |
            [[ -f store-dump ]] && nix-store --import < store-dump
            drv=$(nix-instantiate)
            nix-store -qR $drv | grep '.drv$' | grep -v $drv > deps.txt
            nix-store --realise $(cat deps.txt) > bin-deps.txt
            nix-store --export $(nix-store -qR $(cat bin-deps.txt)) > store-dump
      - save_cache:
          key: v2-nix-store-dump-{{ epoch }}
          paths:
            - store-dump
      - run:
          name: Build GHC
          command: |
            nix-build --cores 2 -A ghc-custom.all
      - run:
          name: Dump artifacts
          command: |
            mkdir dist-cache
            nix copy --to file://dist-cache/ $(readlink result) $(readlink result-doc)
      - store_artifacts:
          path: dist-cache
          destination: dist-cache
